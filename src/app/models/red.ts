export class Red {
    id: number=0;
    red: string;
    link: string;
    icono: string;

    constructor(red:string, link:string, icono:string){
        this.red = red;
        this.link = link;
        this.icono = icono;
    }
}
